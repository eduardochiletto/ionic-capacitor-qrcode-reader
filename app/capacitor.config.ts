import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.joaobreder.qrcodereader',
  appName: 'Ionic Capacitor QR Code Reader',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
