import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.joaobreder.qrcodereader',
  appName: 'QR Code Reader',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
